<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_ecosystem"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_ecosystem"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ecosystem">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Ecosystem">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pantalla_principal"> </activity>
        <activity android:name=".Inicio"> </activity>
        <activity android:name=".PanelSolar"> </activity>
        <activity android:name=".EstadoYMatenimiento"></activity>
        <activity android:name=".BateriaActivity"></activity>
    </application>

</manifest>